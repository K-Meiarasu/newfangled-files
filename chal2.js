const foods = ['pizza', 'burger', 'fingerChips', 'donuts', 'springRoll']; 
const arr=['noodles','icecream']
var start=1,rem=4;
console.log(splice(foods,arr,start,rem))
function splice(foods,arr,start,rem){
    const modifiedArray=[];
    for(let i=0;i<start;i++)
    {
        
    }
    return modifiedArray
}